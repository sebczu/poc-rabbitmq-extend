spring:
  application:
    name: poc-rabbitmq-consumer
  rabbitmq:
    port: 5672
    host: localhost
    username: admin
    password: test

server:
  port: 0

management:
  endpoints:
    web:
      exposure:
        includ: "*"
